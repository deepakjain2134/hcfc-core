
@model HCF.Web.Models.MultiSelectPicker

@if (Model.IsMultiple)
{
    @*@Html.MultiSelectBuildingDropDownList("ddlSinglebuildings", Model.SelectedValue, "Select", new
        {
            @class = "ddlboxLive ddlMultiUser",
            multiple = "multiple",
            data_selected_text_format = "count > 1",
            data_live_search = "true"
        })*@
    <multiselectbuildingdropdownlist name="ddlSinglebuildings"
                                     selectedvalue="@Model.SelectedValue"
                                     firstvalue="Select"
                                     multiple="multiple"
                                     class="ddlboxLive ddlMultiUser">
    </multiselectbuildingdropdownlist>

}
else
{
    <multiselectbuildingdropdownlist name="ddlSinglebuildings"
                                     selectedvalue="@Model.SelectedValue"
                                     firstvalue="Select"
                                     class="ddlboxLive ddlMultiUser">
    </multiselectbuildingdropdownlist>

    @*@Html.MultiSelectBuildingDropDownList("ddlSinglebuildings", Model.SelectedValue, "Select", new
        {
        @class = "ddlboxLive",
        data_selected_text_format = "count > 1",
        data_live_search = "true"
        })*@
}
