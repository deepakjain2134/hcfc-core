@{
    ViewBag.Title = "CMMS Sync";
}


    @section pageHeader{
    <section class="content-header">
        <h1>
            CMMS Sync
        </h1>
    </section>
        }
    <div class="container-fluid">
        <div class="row">
            <div class="settingnewBox">
                @await Html.PartialAsync("_MoreConfig")

                <div class="rightcol-setting">
                    <div class="box">
                        <script>
                            function OnSuccess(data) {
                                if (JSON.parse(data.Result).Success) {
                                    swalalert(JSON.parse(data.Result).Message);
                                } else {
                                    swalalert(JSON.parse(data.Result).Message);
                                }
                            }
                        </script>
                       
                            @*@using (Ajax.BeginForm("TmsRefresh", "Common", new { OnSuccess = "OnSuccess" }))
        {*@

                            <form asp-controller="Common" asp-action="TmsRefresh"
                                 data-ajax-success="OnSuccess"
                                  data-ajax="true" data-ajax-method="POST">

                                @Html.AntiForgeryToken()
                                <div class="sync-cmmsform">
                                    <div class="form-group">
                                        Duration:
                                        <select id="days" name="days" class="form-control" required>
                                            <option value="0">All</option>
                                            <option value="5">5 days</option>
                                            <option value="10">10 days</option>
                                            <option value="15">15 days</option>
                                            <option value="20">20 days</option>
                                            <option value="30">30 days</option>
                                            <option value="45">45 days</option>
                                            <option value="60">2 months</option>
                                            <option value="90">3 months</option>
                                            <option value="120">4 months</option>
                                            <option value="150">5 months</option>
                                            <option value="180">6 months</option>
                                            <option value="365">1 year</option>
                                        </select>
                                    </div>
                                    <div class="form-group">
                                        &nbsp;
                                        <select id="typeCode" name="typeCode" required class="form-control">
                                            <option value="">Select</option>
                                            <option value="@HCF.BDO.Enums.TMSTable.workorder">@HCF.BDO.Enums.TMSTable.workorder</option>
                                            <option value="@HCF.BDO.Enums.TMSTable.AssetMaster">@HCF.BDO.Enums.TMSTable.AssetMaster</option>
                                        </select>
                                    </div>
                                    <br>
                                    <button name="submitButton" class="comm-bluebutton" id="Submit" type="submit" style="margin-top: 21px;">Refresh</button>
                                </div>
                                @*}*@
                                </form>
                       
                    </div>
                </div>
            </div>
        </div>
    </div>


