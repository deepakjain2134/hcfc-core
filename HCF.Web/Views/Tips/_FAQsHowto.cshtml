@model List<HCF.BDO.Tips>
@if (Model.Count != 0)
{
    <div class="accodian-helpBox">
        @foreach (var data in Model.Where(x => x.TipType == HCF.BDO.Enums.TipTypes.FAQHowTo && x.IsActive == true && x.IsApproved == 1))
        {
            <div class="help-panellist" data-menuId="@data.Services.Id" id="index_@data.TipId" rel="@data.TipId">
                <div class="help-panelHeading">
                    <img class="howto-plus" src="https://raw.githubusercontent.com/DataTables/DataTables/1.10.7/examples/resources/details_open.png" />
                    <img class="howto-minus" src="https://raw.githubusercontent.com/DataTables/DataTables/1.10.7/examples/resources/details_close.png" />
                    <h3>@data.Title</h3>
                </div>
                <div class="help-panelDescption">
                    @Html.Raw(data.Description)
                </div>
            </div>
        }
    </div>
}
@*<script>
    $(document).ready(() => {
       debugger;
        var list = $(".help-faqpanellist").map(function () { return $(this).attr("rel"); }).get();
        var id = list[0];
        $('#index_' + id).addClass('panel-active');
    });
    $(document).on('click', '.help-faqpanellist', function () {
        debugger;
        $('.help-faqpanellist').not(this).removeClass('panel-active');
        $(this).toggleClass('panel-active');
    }); 
</script>*@


@*<style>
    .help-faqpanellist {
        background-color: #f0f2f5;
        margin-bottom: 11px;
        border-radius: 6px;
        padding: 3px 0 0;
        border: 1px solid #e4e4e4;
    }
    .help-faqpanellist.panel-active .howto-minus {
            display: block;
    }

    .help-faqpanellist.panel-active .howto-plus {
            display: none;
     }
</style>*@
