@*@model HCF.Web.Models.SelectPicker

@if (Model.IsMultiple)
{
    @Html.EpUserAssignementDropDown("ddlMultiUser", 0, "", new
{
    @class = "ddlboxLive ddlMultiUser",
    multiple = "multiple",
    data_selected_text_format = "count > 1",
    data_live_search = "true"
}, Model.ShowEmail,Model.ShowEpCount)
}
else
{
    @Html.EpUserAssignementDropDown("ddlUsers", Model.SelectedValue, "", new
{
    @class = "ddlboxLive",
    data_selected_text_format = "count > 1",
    data_live_search = "true"
},Model.ShowEmail, Model.ShowEpCount)
}*@
